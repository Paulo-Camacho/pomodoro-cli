#include "Timer.h"
#include "Prompt.h"

int main()
{
    // Timer example(0.05);
    // example.start();  
    // return 0;
    Prompt start("HEY");
    start.question();
}



// Constructors are ok in .h files and destructors. They should init private protected members.
